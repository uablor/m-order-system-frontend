# API Base URL
VITE_API_BASE_URL=http://localhost:4000/api

# API server origin only (scheme + host + port, no path) — used in CSP connect-src
VITE_API_ORIGIN=http://localhost:4000

# Frontend public URL (for customer links in notifications)
VITE_APP_URL=http://localhost:5173
